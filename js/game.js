// To define a global variable, make it GAME.<var_here>
var GAME = {};

$(document).ready(function() {
    // Setup items here

    // KEEP THIS
    setInterval(gameLoop, 500);
});

function gameLoop() {
    //console.log("hello world");
}
